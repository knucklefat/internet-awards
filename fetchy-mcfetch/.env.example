# Auto-Export Configuration
# Copy this file to .env and update with your actual values

# The full URL to your Devvit app's export endpoint
# You can find this by:
# 1. Going to your subreddit with the app installed
# 2. Opening the app
# 3. Clicking "View All Nominations & Stats"
# 4. Clicking "Export All to CSV"
# 5. Copy the URL from the download (without the timestamp)
EXPORT_API_URL=http://localhost:3000/api/export-csv

# Layer 4 (optional): Resolve "Resolved Thing" with an LLM
# OpenAI: get a key at https://platform.openai.com/api-keys — use with --llm
# OPENAI_API_KEY=sk-...
# Gemini: get a key at https://aistudio.google.com/app/apikey — use with --gemini
# GEMINI_API_KEY=your-key
